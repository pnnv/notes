#### variables
```js
const accId = 89 // can't be changed
let accountEmail = "pnus@gmail.com" 
var accPaswd = "pnus29"
accLocation = "R"
```

- const variable can't be modified/reassigned
- rest of the variables can be changed
